- Implement FileService tests

- Implement the memory model of my compiler (the instructions, syntax...)

- Although you can implement a scheduler assuming the output of the compiler (a vector of instructions), it is better to first implement the 
compiler

-Inyect the services in the controller with a configuration factory, depending on the config of the program. Return a service package